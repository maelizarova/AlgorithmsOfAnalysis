dbms_lob.substr(cea.description_claim, 4000, 1) as description_claim,

text_expr = "dbms_lob.substr(cea.description_claim, 4000, 1) as description_claim,"
query_without_text = query.replace(text_expr, "")
