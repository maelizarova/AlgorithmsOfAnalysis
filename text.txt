SELECT *
FROM (
    SELECT
        claim_num,
        created,
        product,
        theme,
        category,
        description_len
    FROM base
    WHERE product = 'Автокредит'
      AND description_len > 4000
    ORDER BY description_len DESC
)
WHERE rownum <= 20;
