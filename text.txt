def _fig_for_clearml(fig, width=900, height=450):
    """Копия для upload: меньше layout, те же данные."""
    fig_log = go.Figure(fig)
    fig_log.update_layout(width=width, height=height, autosize=False)
    return fig_log
# после target_distribution
fig = target_distribution(sample, [TARGET], figsize=(12, 6))
Logger.current_logger().report_plotly(
    title="Распределение таргетов",
    series=TARGET,
    figure=_fig_for_clearml(fig, width=800, height=400),
)
