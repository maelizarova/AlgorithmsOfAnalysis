# имена фичей
names = model.booster_.feature_name()
# importance_type: "split" | "gain"
vals = model.booster_.feature_importance(importance_type="gain")
fi = pd.Series(vals, index=names).sort_values(ascending=False)
