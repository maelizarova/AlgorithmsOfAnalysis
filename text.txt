ID_COLS = PRIMARY_KEY + ['year_month']
FEATURE_COLS = [
    c for c in merged_df.columns
    if c not in ID_COLS + [TARGET]
]
train_feature_universe = train[ID_COLS + FEATURE_COLS].copy()
test_feature_universe = test[ID_COLS + FEATURE_COLS].copy()
oot_feature_universe = oot[ID_COLS + FEATURE_COLS].copy()
train_feature_universe.to_parquet(f"{PATH_MODELS_DATA}/sample_train_feature_universe.parquet")
test_feature_universe.to_parquet(f"{PATH_MODELS_DATA}/sample_test_feature_universe.parquet")
oot_feature_universe.to_parquet(f"{PATH_MODELS_DATA}/sample_oot_feature_universe.parquet")
pd.Series(FEATURE_COLS, name="feature").to_csv(
    f"{PATH_MODELS_DATA}/feature_universe_after_psi.csv",
    index=False,
)
