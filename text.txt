print("Проверяю sample 5 строк с полем description_claim...")
t0 = time.perf_counter()
sample_with_text_query = f"select * from ({query}) q where rownum <= 5"
with engine.connect() as conn:
    source_df = pd.read_sql(text(sample_with_text_query), con=conn)
print(f"Sample с текстом завершён за {time.perf_counter() - t0:.1f} сек. Строк: {len(source_df)}")
display(source_df.head(10))
