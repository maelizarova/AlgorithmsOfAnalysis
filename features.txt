cluster_array = df_plot['cluster'].to_numpy(dtype=str)
    silhouette_series = df_plot.get('silhouette_point')
    if silhouette_series is None:
        silhouette_array = np.full(len(df_plot), np.nan)
    else:
        silhouette_array = silhouette_series.to_numpy(dtype=float)
    hover_array = df_plot['hover_text'].to_numpy(dtype=object)
    custom_data = np.column_stack([cluster_array, silhouette_array, hover_array])
