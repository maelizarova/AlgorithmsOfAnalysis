fig = px.scatter(
        x=df_plot['dim_1'],
        y=df_plot['dim_2'],
        color=df_plot['cluster'],
        title=title,
        height=650,
        custom_data=custom_data,
        labels={'x': 'dim_1', 'y': 'dim_2', 'color': 'cluster'}
    )
