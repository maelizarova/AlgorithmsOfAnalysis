# LDA
lda_model = LdaTopicModel(
    n_topics=15,
    max_df=0.95,
    min_df=15,
    ngram_range=(1,3)
)
# (если долго/память) снизьте max_features у CountVectorizer в классе
# например: CountVectorizer(..., max_features=150_000)

# LSI+KMeans
lsi_model = LsiKMeansTopicModel(
    n_components=250,
    n_clusters=15,
    max_df=0.95,
    min_df=15,
    ngram_range=(1,3)  # для кластера
)
# если словарь слишком большой — добавьте max_features=200_000
