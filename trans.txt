# Все столбцы где есть разные значения для одинаковых ключей
conflicting_cols = [
    col for col in feat_df.columns 
    if col != PRIMARY_KEY 
    and feat_df.groupby(PRIMARY_KEY)[col].nunique().max() > 1
]

print("Конфликтующие столбцы:", conflicting_cols)
