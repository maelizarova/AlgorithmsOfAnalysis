SELECT client_did, report_date, target
FROM (
  SELECT *,
    ROW_NUMBER() OVER (
      PARTITION BY client_did 
      ORDER BY target DESC, report_date DESC
    ) AS rn
  FROM your_table
) t
WHERE rn = 1
